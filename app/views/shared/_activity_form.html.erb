<%= form_for(@activity) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    <%= f.text_area :comments, placeholder: "New activity" %>
  </div>
  <%= f.submit "Post", class: "btn btn-primary" %>
<% end %>
